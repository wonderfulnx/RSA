#include <iostream>
#include <random>
#include <fstream>
#include "src/BigInteger.h"
using namespace std;

int main()
{
    int UNIT_NUM = 128;
    BigInteger a(UNIT_NUM);
    BigInteger b(UNIT_NUM);
    return 0;
}
